import 'package:flutter/material.dart';

class StokBarang extends StatelessWidget {
  const StokBarang({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Hell'),
    );
  }
}
